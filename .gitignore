node_modules/
.DS_Store
Thumbs.db
*.zip

# локальные помощники публикации (не нужны на сайте)
*.cmd
*.ps1

# черновик локализации (интегрируется отдельно)
i18n.draft.json

# локальный документ продвижения (не публиковать)
AvSec-продвижение.md
data.src.js

# ключ AES и приватный GAS с ним — НЕ публиковать
# (data.full.enc — публичный AES-шифр, он КОММИТИТСЯ; бесполезен без ключа)
backend/_generated/
.avsec-fullkey

# Реальные рентген-снимки TIP — НИКОГДА не публиковать в открытом репозитории
# (служебная информация ограниченного доступа + права вендоров). См. xray/README.md
xray/*.jpg
xray/*.jpeg
xray/*.png
xray/*.webp
xray/*.gif
xray/*.bmp
xray/*.tif
xray/*.tiff
xray/scenes.json
